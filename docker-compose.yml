services:
  backend:
    build: .
    container_name: nexscreen-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    command: uvicorn run:app --host 0.0.0.0 --port 8000 --reload
